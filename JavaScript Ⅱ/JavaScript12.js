// オブジェクトを要素に持つ配列

const items = [
    { name: "青いカトレア", price: 1000 },
    { name: "赤いカトレア", price: 800 }
];

// 配列の中のオブジェクトを取り出す
console.log(items[1]);

// 配列の中のオブジェクトの値を取り出す
console.log(items[1].price);